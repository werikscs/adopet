import styled from "styled-components";

export const Container = styled.div`
  text-align: left;

  select {
    border-radius: 25px;
    border: none;
    width: 100%;
    height: 45px;
    display: flex;
    transition: 0.4s;
    margin: 10px 0;
    padding-left: 10px;
    color: var(--color-title);
  }
`;
